## ADCS Applications and Algorithms

The ADCS application owns subsystem operating modes, guidance, control, and the selection of attitude-processing algorithms. It commands the Level 2 ADCS managers through typed interfaces and receives satellite-level policy from `SatStateMachine`.
