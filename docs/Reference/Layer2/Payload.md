# Payload Hardware Managers

For this document, GNSS is treated as a payload/data-collection hardware manager. `GnssManager` owns receiver initialization, UART interaction, fix collection, and the position/time data consumed by higher-level components.

Camera and star-tracker manager designs are TBA.
