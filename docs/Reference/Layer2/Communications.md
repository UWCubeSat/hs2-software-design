# Communications Hardware Managers

`TmtcRadioManager` is the Level 2 adapter for the EnduroSat S-band radio. It translates between the communications topology and the Linux UART driver while preserving the F' communications adapter interface.
