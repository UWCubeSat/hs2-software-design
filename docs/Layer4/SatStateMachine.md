# SatStateMachine

TBA. The Level 4 `SatStateMachine` Software Design Document will define satellite operating modes, transition criteria, typed application-mode ports, and system-level verification once the component design is available on this branch.

`HardwareResetManager` is not included in this level because it is planned as a separate system-infrastructure responsibility rather than mission orchestration.
